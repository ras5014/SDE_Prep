// Union
let product_id: string | number;
product_id = 22;

// Enum
enum ProductStatus {
  Active,
  Inactive,
}
let product_status: ProductStatus = ProductStatus.Active;
console.log(product_status);
